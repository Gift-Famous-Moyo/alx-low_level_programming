#include <stdio.h>

/**
 * main - prints the phrase "Programming is like building a multilingual puzzle"
 *
 * return: Always 0 (if exited properly, none zero otherwise) 
 */

int main(void)
{

	puts("\"programming is like builing a multilingual puzzle");
	return: (0);
}
